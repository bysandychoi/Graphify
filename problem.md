# Graphify — Problem Definition

작성일: 2026-09-16  
상태: 업무 인터뷰를 바탕으로 한 문제 정의. 최종 구현 명세가 아니며, 실제 회사 데이터의 구조는 아직 확인하지 않았다.

## 1. 해결하려는 문제

제조 스케줄링과 AI/GNN 관련 실험에 사용하는 복잡한 현장 데이터를, 중요한 관계와 분포를 보존하는 작은 데이터로 줄이고 싶다.

과거에는 원본 관계 데이터를 참조하지 않고 무작위로 데이터를 생성했다. 코드 오류로 특정 resource가 모든 설비에서 가능한 것처럼 표현되었고, 잘못된 데이터로 학습이 진행됐다. 사용자는 이상한 스케줄링 결과를 보고 생성 데이터를 직접 살펴본 뒤 오류를 발견했다.

생성 코드는 수정했지만, 수정된 데이터가 원본의 특성과 관계를 충분히 대표하는지 판단하기 어렵다. 핵심 문제는 단순한 데이터 생성이 아니라 **축소 데이터가 무엇을 보존하고 무엇을 잃었는지 확인하기 어렵다는 것**이다.

## 2. 사용자와 업무 상황

- 사용자: 제조 스케줄링을 개발하고 결과를 검토하는 담당자.
- 사용 시점: 원본보다 작은 데이터로 실험하기 전, 그리고 생성·축소 데이터의 타당성을 확인할 때.
- 현재 불편: 스케줄링 결과가 이상해진 후 입력 데이터를 직접 뜯어보며 관계 오류를 찾아야 한다.
- 확인된 영향: 잘못된 관계가 학습에 반영되었으며, 데이터 생성 코드 수정이 필요했다.
- 미확인 영향: 소요 시간, 재학습 비용, 현장 운영 손실은 정량 확인하지 않았다.
- 회사 데이터는 외부로 반출할 수 없다. 외부 개발·검증에는 가상 데이터를 사용하고, 실제 회사 데이터 테스트는 사용자가 회사 환경에서 수행한다.

## 3. 인터뷰에서 확인한 데이터의 의미

### lot_data

- 입력 lot은 이미 필요한 step으로 필터링된 상태다.
- lot에는 **현재 진행할 step**에서 할당 가능한 `(eqp, resource)` 쌍이 있다.
- 예: `(M01, R01), (M02, R02)`는 두 쌍이 가능하다는 뜻이며, `(M01, R02)`까지 가능하다는 뜻이 아니다.
- lot의 할당 가능 설비 수는 가능 쌍에 나타나는 **중복 없는 eqp 수**다.
- 서로 다른 resource가 같은 설비와 연결되어도 설비 수를 중복 계산하지 않는다.
- lot의 현재 가능 쌍만으로 이후 step의 가능 쌍을 알 수 있다고 가정하지 않는다.
- lot 데이터에 납기는 없다. 우선순위는 대화 중 분석 속성으로 논의했지만 실제 필드 존재 여부와 값 체계는 미확인이다.

### run_spec / eqp_step / resource_data

- `run_spec`과 `eqp_step`은 전체 사양이며, 모든 사양을 결과에 표현할 필요는 없다.
- `eqp_step`에서는 공정·step별 resource와 가능 설비의 관계를 확인할 수 있다.
- 예: 같은 S01이라도 R01은 M01·M02에서 가능하고, R02는 M03에서만 가능할 수 있다.
- 이번 분석의 기준은 입력 `lot_data`이며, 필요한 사양은 lot과의 연결을 통해 찾아낸다.
- `resource_data`의 실제 필드와 연결 키는 미확인이다.

### 위치와 층

- lot 현재 위치와 설비 층은 별도 데이터를 연결해야 파악할 수 있다는 전제로 진행한다.
- 한 lot의 후보 설비들이 서로 다른 층에 있을 수 있으며, 이 특성이 중요하다.
- 실제 연결 테이블, 키, 위치에서 층을 구하는 방식은 아직 확정하지 않았다.

## 4. 목표와 1차 범위

**원본에서 lot 표본을 선택하고 관련 데이터를 함께 축소하되, 원본 입력 형식과 선택된 lot의 모든 가능 쌍을 유지한다. 원본과 축소 후보의 차이를 보여주고 사용자가 선택하게 한다.**

초기에는 별도 그래프 입력 생성이 목표였으나, 인터뷰를 통해 1차 결과물을 **원본 입력 형식 그대로의 축소 데이터**로 변경했다. 관계를 분석하되 새로운 그래프 파일 형식으로 변환하는 것은 이번 요구사항이 아니다.

포함 범위:

- lot–설비–resource의 할당 가능 관계.
- lot 위치·설비 층과 그 관계.
- 원본 분포 분석, 표본 후보 생성, 전체 비교표.
- 연결된 연관 데이터 축소와 일관된 ID 변경.
- 원본 ID와 변경 ID의 대응표.

제외 범위:

- GNN 학습, RL, Scheduler 개발, 성능 최적화.
- 별도 그래프 스키마 또는 학습 프레임워크 전용 입력 생성.
- 현재 확인한 범위를 넘어서는 관계의 임의 추가.
- 회사 원본 데이터의 외부 반출.

## 5. 사용자가 확정한 축소 원칙

### 표본 선택과 분포

1. 무작위로 새 lot을 만들어내지 않고 **원본 lot 중에서 선택**한다.
2. 목표 크기는 lot 수 또는 비율 중 하나로 지정할 수 있게 한다.
3. 전체 입력 lot을 기준으로, 할당 가능 설비 수의 **정확한 개수별 분포**를 유지한다. 구간별이 아니며 공정·step별로 각각 맞추는 것도 아니다.
4. 선택에 반영할 속성을 config에서 지정하고, 지정한 속성의 원본 분포를 유지한다. 특정 속성값을 우대한다는 의미가 아니다.
5. 속성 간 조합도 config에서 각각 지정한다. 예를 들어 `할당 가능 설비 수 × 우선순위`처럼 함께 나타나는 비율을 반영한다. 실제로 없는 속성은 사용하지 않는다.
6. 선택한 모든 속성을 자동으로 하나의 거대한 조합으로 묶지는 않는다.
7. 표본을 바꿔 여러 결과를 비교할 수 있어야 한다.

### 위치·층 분포

다음 특성을 축소 후보에서도 반영한다.

- 후보 설비가 한 층에만 있는 lot / 여러 층에 있는 lot의 비율.
- lot의 현재 층에서만 처리 가능 / 다른 층에서만 처리 가능 / 현재 층과 다른 층 모두에서 처리 가능한 lot의 비율.
- 선택한 lot에 후보 설비가 여러 개 있으면 각 후보와 층 정보를 모두 유지한다. 대표 설비나 대표 층 하나로 대체하지 않는다.

### 드문 유형과 관계 보존

- 드문 설비·resource 조합이라는 이유만으로 해당 lot을 반드시 선택할 필요는 없다.
- 특정 할당 가능 설비 수 유형이 축소 표본에서 사라져도 허용하되, 사라진 유형을 결과에 표시한다.
- **일단 선택한 lot의 가능 쌍은 모두 보존한다.** 관계를 잘라 설비 수를 줄이지 않는다.
- lot·설비·resource를 같은 비율로 줄일 필요는 없다. 선택된 lot의 관계 보존이 우선이다.

### 연관 데이터 축소와 ID 변경

- 선택한 lot에서 연결을 따라가며 필요한 설비·resource·위치·사양 항목을 남긴다.
- 선택한 lot과 연결되지 않는 항목은 제외한다.
- 원본 입력 형식은 유지한다.
- 변경 대상의 ID를 일관되게 바꾸고, 데이터 간 참조도 함께 갱신한다.
- lot뿐 아니라 **변경하는 모든 ID의 원본 ↔ 변경 대응표**를 실행 환경 안에 별도로 남긴다.
- 실제 ID 필드와 연관 데이터 보존 경계는 스키마 확인 후 정한다. 공통 코드나 분류값을 임의로 ID로 취급하지 않는다.

## 6. 사용자가 판단하는 흐름

1. 입력 데이터와 설정을 읽고 관계 및 필요한 위치·층 연결 정보를 확인한다.
2. 원본의 관계·속성 분포를 분석한다.
3. 목표 lot 수를 맞춘 후보와, lot 수를 늘려 원본 분포에 더 가까워지도록 한 후보를 제안한다.
4. 늘릴 lot 수의 상한은 미리 확정하지 않는다. 제안 크기와 개선 정도를 보고 사용자가 결정한다.
5. 요약보다 **전체 비교표를 먼저** 보여준다.
6. 사용자가 후보를 선택한 뒤에만 최종 축소 데이터와 ID 대응표를 생성한다.

분포 차이의 허용 기준은 아직 수치로 정하지 않았다. 자동으로 합격 여부를 판정하기보다 차이를 보여주고 사용자가 판단한다. lot 수를 늘리면 반드시 모든 분포가 개선된다고 가정하지 않으며, 실제 차이를 비교해야 한다.

### 비교에서 보여줄 정보

- 원본과 후보별 lot·설비·resource 수.
- 설정한 모든 속성 및 속성 조합의 개수·비율과 원본 대비 차이.
- 사라진 유형.
- 관계에 해당하는 lot의 비율과 가능한 관계 조합의 비율 모두.
- lot 정보와 전체 사양의 충돌 내역.

관계별 비율은 중복 가능성과 분모를 명확히 표시해야 한다. 하나의 lot이 여러 관계에 해당할 수 있으므로 모든 관계 비율의 합이 항상 100%라고 가정하지 않는다. 구체적인 관계 조합 단위와 분모는 미결 사항이다.

개별 lot의 후보 관계를 펼쳐보는 기능은 현재 필수 요구가 아니다. 위 비교 정보로 우선 판단한다.

## 7. 오류·불일치 처리 정책

### lot 정보와 전체 사양의 충돌

- `lot_data`에 있는 가능 쌍이 `eqp_step`에 없더라도 **lot 정보를 우선하여 유지**한다.
- 충돌 내역을 사용자에게 보여준다.
- 이를 이유로 원본 사양을 자동 수정하거나 새로운 사양 행을 만들어 넣는 정책은 정하지 않았다.

### 위치·층 정보 누락

- 필요한 위치·층 연결 정보를 찾지 못하면 누락 항목을 보여주고 확인을 요청한다.
- 확인·보완 전에는 표본 선택을 진행하지 않는다.

### 아직 정하지 않은 오류 정책

- 중복 ID, 존재하지 않는 resource·설비 참조, 빈 가능 쌍, 잘못된 config 등은 테스트 대상으로 다루되, 실제 처리 정책은 별도로 확정해야 한다.
- 모르는 값을 추정해 채우거나 정상 데이터로 간주하지 않는다.

## 8. 가상 데이터 검증 계획

사용자는 큰 가상 데이터를 만들고 여러 경우를 테스트하기를 요청했다. 현재 가상 데이터 생성·테스트는 아직 실행하지 않았다.

초기 가상 데이터 규모 제안은 lot 10,000개·설비 80대·resource 24개다. 이는 개발용 제안이며 회사 데이터의 실제 크기나 확정된 기준이 아니다.

| 사례 | 확인하려는 내용 |
| --- | --- |
| 설비 수가 다양한 lot | 정확한 설비 수별 원본·후보 분포 비교 |
| 같은 설비와 여러 resource 쌍 | 설비 수 중복 계산 방지, 쌍 전체 보존 |
| 드문 설비 수 유형 | 표본에서 빠진 유형 표시 |
| 같은 층·여러 층 후보 | 후보와 층 정보를 모두 유지 |
| 현재 층·다른 층 후보 | 세 위치 관계 유형의 비율 비교 |
| 여러 속성·조합 | config로 지정한 분포 차이 비교 |
| lot과 eqp_step 불일치 | lot 가능 쌍 유지, 충돌 표시 |
| 위치·층 연결 누락 | 선택 전 중단과 확인 요청 |
| 여러 표본 | 표본 변경에 따른 차이 비교 |
| 목표 수·목표 비율 | 두 입력 방식으로 후보 생성 |
| 관련 데이터 축소 | 선택 lot의 관계 보존과 미연결 항목 제외 |
| ID 변경 | 모든 변경 ID의 대응표와 참조 일관성 |
| 후보 미선택 상태 | 최종 데이터가 확정 출력되지 않는지 확인 |

가상 데이터용 스키마와 기대 결과는 실제 회사 스키마와 구분해 기록한다. 실제 데이터 테스트와 스키마 적합성 확인은 사용자가 회사 환경에서 수행한다.

## 9. 개발·실행 환경과 일정

- 개발은 Claude와 진행한다.
- 실제 실행 환경에서는 Claude를 사용할 수 없고 Codex를 사용할 수 있다.
- **2026-09-21까지 실제 실행 환경에서 실행·검증하는 것이 목표**다. 현재 완료된 상태가 아니다.
- 코드와 문서만으로 인수인계할 수 있는 최소 하네스를 적용한다.
- 공통 작업 규칙, 실행 방법, 가정, 검증 결과, 미결 사항을 저장소에 남긴다.
- 실제 환경의 OS, 사용 가능한 런타임, 패키지 설치 제한, 코드 반입 방법과 접근 시점은 미확인이다.

## 10. 미결 질문과 구현 전 확인 사항

- 실제 입력 파일 형식, 필드, ID, 관계 키와 참조 방향.
- 필요한 사양 항목을 보존하는 정확한 단위와 연결 탐색 범위.
- 실제 존재하는 선택 속성과 값 체계. 우선순위 존재 여부 포함.
- 관계 조합의 정의, 관계별 lot 비율과 조합 비율의 분모.
- 여러 분포가 충돌할 때 후보를 만드는 구체적인 방법과 개선 정도의 지표.
- 비율로 지정한 목표의 정수 반올림 방식, 최소·최대 lot 수 처리.
- 표본 변경과 재현을 위한 seed 설정·기록 방식. 기록은 제안 상태다.
- 원본과 같은 수를 선택하는 경우 등 개선 후보가 더 이상 없는 상황의 표시 방식.
- 위치·층 누락 외 입력 오류의 상세 처리 정책.
- config 구조, 실행 인터페이스와 결과 파일 구성.

이 문서는 문제와 업무상 결정을 정리한다. 알고리즘, 코드 구조, 최종 스키마를 확정하는 문서로 사용하지 않는다.
