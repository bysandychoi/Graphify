# HANDOFF

> 이 문서는 프로젝트의 현재 상태 스냅샷이다. 각 phase(P0, P1, ...)가
> 끝날 때마다 그 phase의 마지막 작업이 아직 `in_progress`인 동안 갱신한다
> (`AGENTS.md` "작업 흐름" 5단계 참고). 문서 구조·작업 규칙은 `AGENTS.md`,
> 문제 정의는 `problem.md`, 작업 목록은 `backlog.json`(CLI로만 조회)이
> 단일 출처다. 아래 "미결 사항"의 목록은 problem.md 10절을 그대로
> 옮긴 사본이다(요약이 아님) — 원문이 바뀌면 이 목록도 함께 갱신해야
> 한다.

## 현재 상태

**구현 시작 전.** 지금까지 끝난 것은 문서/스키마 설계뿐이며, 실제
가상 데이터 생성 로직이나 축소 로직 코드는 아직 없다.

- P0(저장소 최소 하네스, T001~T005): 전부 완료 — 문서 구조 설계(T001),
  `README.md`(T002), `AGENTS.md`(T003), `CLAUDE.md`(T004), 이 문서
  초안(T005)까지 마쳤다. (`problem.md`는 P0 backlog 작업의 산출물이
  아니라 그 이전에 이미 존재하던 문제 정의 문서다.)
- P1(가상 스키마 정의, T006~T010): 전부 완료 — `lot_data`/
  `eqp_step_run_spec`/`resource_data`/`location_floor`와 회사 실제
  스키마를 구분하는 안내 문서까지 전부 작성됨(`docs/virtual_schema/`).
- P2(가상 데이터 생성기, T011~T025): T011(생성기 config 구조 설계,
  `docs/generator/config.md`) 진행 중, 나머지는 `todo`. 실제 생성
  로직(설비·resource 마스터, 가능 쌍 생성, 각종 케이스 주입, 스모크/
  대규모 실행)은 아직 코드로 존재하지 않는다.
- P3~P8(분포 계산, 표본 선택, 비교표, 연관 데이터 축소·ID 변경, 오류
  처리, 테스트)의 모든 작업은 `todo`(착수 가능) — 단, T054(P7, "기타
  입력 오류 탐지", `needs_decision`)는 처리 정책이 없어 사용자 결정을
  먼저 기다린다.
- P9(T068~T078, 11건)는 전부 `waiting`(회사 데이터 확인 필요) 또는
  `needs_decision`(사용자 결정 필요) 상태다 — 아래 "미결 사항" 참고.
- P10(실제 환경 이관, T079~T083)은 T080/T081만 `todo`이고, T079/T082/
  T083은 `waiting`이다. **T079("Codex 실행 환경 요구사항 정리")와
  T078(P9, "실행 환경 제약")이 모두 미확인 상태라, 2026-09-21 목표
  실행에 쓸 실제 환경 자체가 아직 파악되지 않았다** — 이 점이 현재
  가장 중요한 미확인 사항이다.

## 검증 결과

없음. 가상 데이터 생성도, 실제 회사 데이터 테스트도 아직 실행하지
않았다(problem.md 8절).

## 가정

- 개발은 Claude로, 실제 실행 환경 검증은 Codex로 진행한다(problem.md 9절).
- 회사 데이터는 외부로 반출할 수 없다(problem.md 2절) — 즉 회사
  데이터를 이 저장소로 가져올 수 없다. 그래서 개발·검증은 전부 가상
  데이터로 하고, 실제 데이터 테스트는 사용자가 회사 환경에서 직접
  수행한다.
- 가상 데이터 스키마(`docs/virtual_schema/`)의 필드명·키·직렬화
  형식은 전부 개발 편의상 임의로 정한 것이며, 회사 실제 구조를
  추정한 것이 아니다(각 스키마 문서의 "표현하지 않는 것" 절 참고).
- 가상 데이터 초기 규모 제안(lot 10,000 / 설비 80 / resource 24)은
  개발용 제안치이며 회사 기준이 아니다(problem.md 8절).

## 다음 작업

`python tools/backlog_cli.py list --status todo`로 항상 최신 목록을
확인한다(이 문서는 특정 시점의 요약일 뿐 최신 상태의 단일 출처가
아니다). 이 문서를 갱신하는 시점(2026-09-17) 기준으로는:

1. T002/T005(이 문서)를 커밋하고 `set-status done`으로 마무리한다.
2. T011(생성기 config 설계)의 남은 피드백 반영을 마무리하고 완료한다.
3. P2의 나머지 생성기 구현 작업(T012~T025)을 진행한다 — 의존 관계는
   순수 직선 체인이 아니라 T012→T013→T014까지 순차 진행한 뒤,
   T014 완료 시점에 T015/T016/T018/T020/T022 다섯 갈래가 동시에
   열리는 다이아몬드 구조다(`python tools/backlog_cli.py list
   --depends-on T014`로 확인). 이 다섯 갈래는 서로 다른 케이스
   주입 로직이라 내용 작성은 병렬화 여지가 있지만, `backlog.json`
   갱신(CLI의 start/set-status)은 파일 잠금이 없어 동시 호출 시
   경쟁 상태가 생길 수 있으므로 CLI 호출 자체는 직렬화한다. 이후
   T023(스모크 실행)이 T014~T022 아홉 작업을 모두 모으고,
   T024→T025로 이어진다.
4. P3(분포 계산)~P8(테스트)까지 진행하되, T054(P7, `needs_decision`)는
   사용자 결정 전에는 착수하지 않는다.
5. P9의 [결정 필요]/[확인 필요] 항목은 사용자 결정이 필요하므로
   해당 시점에 사용자에게 확인을 요청한다.
6. P10(실제 환경 이관)은 2026-09-21 목표에 맞춰 준비하되, T079/T078이
   가리키는 "실제 실행 환경이 아직 파악되지 않음" 문제부터 먼저
   해소해야 한다(problem.md 9절).

## 미결 사항

problem.md 10절 "미결 질문과 구현 전 확인 사항"을 그대로 옮긴 목록이다
(문서 상단 안내 참고 — 요약이 아니라 사본이므로 원문이 바뀌면 갱신
필요):

- 실제 입력 파일 형식, 필드, ID, 관계 키와 참조 방향.
- 필요한 사양 항목을 보존하는 정확한 단위와 연결 탐색 범위.
- 실제 존재하는 선택 속성과 값 체계(우선순위 존재 여부 포함).
- 관계 조합의 정의, 관계별 lot 비율과 조합 비율의 분모.
- 여러 분포가 충돌할 때 후보를 만드는 구체적인 방법과 개선 정도의 지표.
- 비율로 지정한 목표의 정수 반올림 방식, 최소·최대 lot 수 처리.
- 표본 변경과 재현을 위한 seed 설정·기록 방식. 기록은 제안 상태다.
- 원본과 같은 수를 선택하는 경우 등 개선 후보가 더 이상 없는 상황의
  표시 방식.
- 위치·층 누락 외 입력 오류의 상세 처리 정책.
- config 구조, 실행 인터페이스와 결과 파일 구성.

위 10개 항목은 `backlog.json`의 P9 작업(T068~T078, 11건)으로 추적되며,
그 중 **T078**("실행 환경 제약")은 problem.md 10절 목록 자체에는 없고
9절(178행, "실제 환경의 OS, 런타임, 패키지 설치 제한, 코드 반입 방법과
접근 시점은 미확인")에서 온 별도 항목이다 — `python tools/backlog_cli.py
list --phase P9`로 전체 11건을 확인한다. 이 밖에도 T054(P7,
"기타 입력 오류 탐지", `needs_decision`)와 T079/T082/T083(P10,
`waiting`)이 사용자 확인·결정을 기다리는 상태다 — 위 "현재 상태" 참고.
