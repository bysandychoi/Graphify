# T010 가상 스키마 vs 회사 스키마 구분 문서화

- 단계: P1 (가상 스키마 정의 (회사 스키마와 구분))
- 상태: in_progress (작업중)
- 예상 소요: 15분
- 선행 작업: T006, T007, T008, T009
- 참고 problem.md: 2절, 8절

## 설명

가상 스키마가 개발/검증용이며 회사 데이터 구조와 무관함을 문서에 명시한다.

## 완료 기준

- 가상 스키마 문서 상단에 구분 문구 포함

## 쉬운 설명

T010은 T006-T009에서 생성한 4개의 가상 스키마 문서(lot_data, eqp_step_run_spec, resource_data, location_floor)가 모두 개발·검증용 가상 스키마이며 회사의 실제 데이터 구조와 무관함을 명시적으로 구분하는 작업입니다.

각 문서의 상단에는 이미 '**이 문서는 개발·검증용 가상 스키마이며 회사 실제 데이터 구조가 아니다.**'라는 구분 문구와 함께, 실제 필드/키/값 체계가 미확인임을 명시하고 있습니다. 이러한 구분 문구는:
- problem.md 2절의 '회사 데이터는 외부로 반출할 수 없으므로 외부 개발·검증에는 가상 데이터를 사용'한다는 원칙과 
- problem.md 8절의 '가상 데이터용 스키마와 기대 결과는 실제 회사 스키마와 구분해 기록한다'는 요구사항을 
구현하는 것입니다.

완료 기준 '가상 스키마 문서 상단에 구분 문구 포함'을 확인하고, 각 문서가 일관되게 이 원칙을 따르고 있음을 검증하는 것이 이 작업의 범위입니다.

## 관련 파일

- `problem.md` — 가상과 회사 스키마 구분의 필요성(2절), 가상 데이터 테스트 원칙(8절)
- `backlog/T006.md` — 선행 작업 - lot_data 스키마 정의
- `backlog/T007.md` — 선행 작업 - eqp_step/run_spec 스키마 정의
- `backlog/T008.md` — 선행 작업 - resource_data 스키마 정의
- `backlog/T009.md` — 선행 작업 - 위치·층 연결 스키마 정의
- `docs/virtual_schema/lot_data.md` — T006 산출물, 상단에 가상 스키마 구분 문구 포함
- `docs/virtual_schema/eqp_step_run_spec.md` — T007 산출물, 상단에 가상 스키마 구분 문구 포함
- `docs/virtual_schema/resource_data.md` — T008 산출물, 상단에 가상 스키마 구분 문구 포함
- `docs/virtual_schema/location_floor.md` — T009 산출물, 상단에 가상 스키마 구분 문구 포함
- `docs/virtual_schema/README.md` — 가상 스키마 인덱스/구분 원칙 확인 산출물(이 문서)

## 피드백 로그

### 2026-09-16

docs/virtual_schema/README.md 리뷰: (1) L28-30 '네 문서 모두 resource_data의 exact match 참조 규칙을 공유하며 각 문서에 명시돼 있다'는 거짓 — lot_data.md/eqp_step_run_spec.md에는 대소문자·exact match 언급이 0건이고, location_floor.md L23-24가 '이 두 문서가 이 규칙을 선언한 것은 아니다'라고 정반대로 적어놨다. L21 표의 '(문자열 exact match)'도 같은 허위 전제 위에서 T008 완료 기준을 충족된 것처럼 제시한다(resource_data.md L23-25는 한 문서 선언만으로 증명되지 않는다고 경고). (2) L31-33 'ID 변경 대상임을 각 문서가 개별적으로 명시한다'도 거짓 — T048/T049 언급은 resource_data.md, location_floor.md에만 있다. 게다가 lot_id/eqp_id/resource_id를 단정하면서 location_id만 미확인 처리 — problem.md L104는 '실제 ID 필드는 스키마 확인 후 정한다'이므로 단정 범위가 과하다. (3) L43-45가 오류 케이스를 전부 T054(미정)로 뭉갬 — location_floor.md L37-46/L89-97의 '이미 정해진 정책'(T053, 확인 전 표본 선택 중단)이 통째로 누락돼 README만 읽으면 오류 처리가 전부 미정인 것으로 오독된다. (4) L35-37 '공백' 목록이 eqp_step_run_spec.md L63-71의 유일한 차단 사항(조인 키 비결정성, '결정적으로 구현 불가')을 빠뜨렸다. (5) L26-27 'lot_data가 참조할 때만 조회'는 과장 — resource_data는 eqp_step도 참조하고, eqp_step_run_spec.md L20-24는 탐색 범위 자체가 미결이라고 못박았다. location_floor는 관계도에서 누락. (6) L10-13 '이 README는 구분 문구를 옮겨 적지 않는다, 실제 문구는 각 문서에만 있다'가 L3-7의 자체 배너와 자기모순. (7) 중복 기재 금지 위반: 완료 기준 요약 열은 CLI 전용 backlog.json의 acceptance_criteria 사본이고 이미 (1)처럼 드리프트했으며, 문서 목록 표는 AGENTS.md L34 구성 요소와 갱신 지점이 이중화됐다(설비 마스터 스키마 추가 시 즉시 발생). 참조 링크와 T047-T054 id는 전부 실재함을 확인했고, L4-7의 problem.md 2절/8절 인용은 정확하다.

