# T012 설비·resource 마스터 생성 로직

- 단계: P2 (가상 데이터 생성기)
- 상태: in_progress (작업중)
- 예상 소요: 30분
- 선행 작업: T011
- 참고 problem.md: 8절

## 설명

설비 80대, resource 24개, 설비별 층 배정을 생성한다.

## 완료 기준

- 설비 id/층, resource id가 중복 없이 생성됨

## 쉬운 설명

T012는 가상 제조 데이터 생성 프로젝트의 설비·리소스 마스터를 생성하는 작업입니다.

구체적으로:

1. **생성 대상:**
   - 설비(eqp): config에서 지정된 수만큼 생성(기본값 80대). 각 설비에 고유한 ID와 층 번호를 부여합니다.
   - 리소스(resource): config에서 지정된 수만큼 생성(기본값 24개). 각 리소스에 고유한 ID를 부여합니다.
   - 층 정보: config의 floor_count로 지정된 층 수 범위 내에서 각 설비에 층 번호를 배정합니다.

2. **생성 결과 (출력 데이터):**
   - eqp_floor: 각 설비의 ID와 층 정보를 담은 테이블
   - resource_data: 각 리소스의 ID(및 선택적 속성)을 담은 테이블

3. **완료 기준:**
   - 설비 ID/층, 리소스 ID가 모두 중복 없이 생성됨
   - 생성된 설비/리소스 수가 config 지정값과 일치함
   - 각 설비의 층 번호가 1 이상 floor_count 이하의 유효한 범위 내에 있음

## 관련 파일

- `docs/generator/config.md` — T011 산출물 - 생성기가 받을 config 구조(scale.eqp_count, scale.resource_count, scale.floor_count)
- `docs/virtual_schema/location_floor.md` — T009 산출물 - eqp_floor 테이블 구조와 설비 층 정의
- `docs/virtual_schema/resource_data.md` — T008 산출물 - resource_data 스키마 구조
- `problem.md` — 문제 정의 8절(가상 데이터 검증 계획) - 규모 기본값(설비 80, resource 24) 근거

## 피드백 로그

### 2026-09-17

리뷰(adversarial): [1] config.md 106-110행이 T012에 명시적으로 요구한 '기본 config 파일'이 없음 - 저장소에 생성기 config JSON이 전혀 없다(누락된 요구사항). [2] config.md 86-91행이 T012에 위임한 seed 소비 방식(단일 Random 공유 vs 단계별 파생) 결정을 코드가 사실상 골랐으나(rng 인자) 어디에도 기록 안 함 - 후속 단계가 같은 스트림을 쓰면 draw 하나만 늘어도 전체 재현 결과가 깨진다. [3] config.md 289행 'scale.*_count 정수 아니면 에러'를 절반만 구현: <=0만 막고 정수 검사 없음. eqp_count=3.0 -> range에서 raw TypeError, eqp_count=True -> len('True')=4로 M0001 한 대를 조용히 생성. seed 누락은 bare KeyError. config.md 255-257행은 정적 검증을 별도 작업으로 분리하라고 했으므로 - 인라인 반쪽 검사를 빼든가 완성하든가 택일 필요. [4] 층 커버리지 무보장: randint 균등 배정이라 config.md 스모크 예시(eqp_count=8/floor_count=3)에서 2000 seed 중 241개(12%)가 빈 층 발생 -> T018 multi_floor 실현 여지를 seed 운에 맡김. floor_count>eqp_count도 무검증. [5] eqp_id 자릿수가 eqp_count 함수(max(2,len(str(n))))라 스케일 바뀌면 같은 1번 설비가 M01/M001로 달라짐 - eqp_id는 exact match 조인 키인데 이 규칙이 문서/주석 어디에도 없음(매직 넘버 2 포함). [6] 출력 키 'resource_data' vs resource_data.md 예시 JSON의 'resources' 불일치. [7] 테스트 0건 - 완료 기준 '중복 없이'는 range 구조상 참이지만 검증 코드가 없고, T021이 행 삭제/null화를 시작하면 'eqp_id마다 정확히 1행' 불변식을 지킬 회귀 방어가 없다. [8] 모듈 docstring이 '설비 마스터'라고 하지만 실제 산출물은 eqp_floor뿐 - eqp_step_run_spec.md 93-94행이 설비 마스터를 '표현하지 않는 것'으로 못박은 것과 용어 충돌.

