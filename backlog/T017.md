# T017 드문 설비 수 유형 케이스 주입

- 단계: P2 (가상 데이터 생성기)
- 상태: in_progress (작업중)
- 예상 소요: 20분
- 선행 작업: T015
- 참고 problem.md: 8절

## 설명

전체에서 극히 드물게 등장하는 설비 수 값을 가진 lot을 소수 포함한다.

## 완료 기준

- 드문 설비 수 값이 1~수 개 lot에서만 등장

## 쉬운 설명

T015에서 만든 '할당 가능 설비 수별 분포'(eligible_eqp_count_distribution)에 추가로, 특정 설비 수 값(예: 5개)을 극히 드물게 나타나도록 한다.

구체적으로:
1. config에서 rare_eligible_eqp_count_types로 '드문' 설비 수 값과 그 범위(예: 5개 설비 → 1~5개 lot)를 절대 건수로 지정한다
2. 그 값은 eligible_eqp_count_distribution과 겹칠 수 없다(상호 배타적)
3. T015의 eqp_count_distribution.py를 고쳐서, rare_eligible_eqp_count_types로 먼저 일부 lot을 떼어내고, 남은 'na머지' lot만 eligible_eqp_count_distribution이 비율로 배정하도록 한다
4. 결과: 1~수 개 lot에서만 드문 설비 수 값이 등장한다

## 관련 파일

- `docs/generator/config.md` — case_ratios.rare_eligible_eqp_count_types 필드 정의 (199행, 절대 건수 형식 설명)
- `generator/eqp_count_distribution.py` — T015 산출물, docstring에 'T017 관련 주의' 섹션 있음 (34~40행)
- `problem.md` — 8절 가상 데이터 검증 계획, '드문 설비 수 유형' 사례 (157행)
- `backlog/T015.md` — 선행 작업 (이미 완료), generator/eqp_count_distribution.py 산출

## 피드백 로그

### 2026-09-17

리뷰(적대적): (1) 스펙 객체 검증이 config.md와 정반대 - _rare_count가 min_count 기본값 0이라 {'5':{}}/{'5':{'max_count':5}}/오타 'mincount' 모두 조용히 0건 = 드문 유형이 아예 안 생겨 완료 기준('1~수 개 lot에서만 등장') 미달(실측). config.md:291-294는 빈 객체와 미지 구조 키를 에러로 못박음. T016 리뷰 항목5와 같은 계열 결함 재발. (2) max_count가 사문화 - 항상 min_count만 배정하므로 config.md:394가 '5~8건'이라 설명하고 :199가 'max_count 없으면 상한 없음/작은 max_count를 지정해야 한다'고 권고한 의미가 사라진다. {'min_count':0,'max_count':5}(문서상 0~5건)는 항상 0건. 결정 근거를 모듈 docstring에만 적고 config.md는 그대로 둬 문서-코드가 모순 상태. (3) rng 공유 계약 미검증 재발 - pipeline.py rare 분기에서 rng=rng를 빼도 138개 테스트 전부 통과(뮤테이션 실측). test_pipeline:186의 공유 검증은 T014 경로만 덮는다(backlog/T015.md:58 지적 그대로). (4) 키 정규화 없음 - 'key in distribution' 문자열 비교라 rare '5' vs distribution '05'가 배타성 검사를 통과해 5가 11건 생성(실측), rare_types 안에서도 '5'/'05'는 rare_counts[value] 덮어쓰기로 건수가 조용히 사라짐. (5) 공유한 make_lot_for_target의 에러 메시지가 rare 실패를 'eligible_eqp_count_distribution asks for a value eqp_step cannot supply'로 오지정(실측) - parse_value_key에는 field_name을 넣어놓고 여기만 누락. (6) remaining==0이면 allocate_target_counts를 아예 안 불러 distribution 합계!=1/잘못된 키가 무검증 통과(실측). 정적 config 검증기가 없어 이 경로가 유일한 방어선. (7) rare_types가 {}면 파이프라인 전체 ValueError - config.md 에러 목록에 없는 규칙(6과 정반대 방향). dict가 아니면 .items() AttributeError. (8) 테스트 공백: rare+shared_eqp_multi_resource 조합(config.md 예시 363/420이 둘 다 켠다) 미검증(수동 확인 결과 T016이 count를 보존해 현재는 안 깨짐), rare 값 '0'(빈 candidate_pairs) 미검증, min_count 미지정/0이 no-op이라는 사실을 잡는 테스트 없음. 통과 확인: T015 회귀 없음(refactor 전 모듈과 3개 분포에서 출력·rng 소비 상태까지 동일, 실측), 분모 공식(유효 lot = rare 합 + 나머지), scale.eqp_count 이하 키 검증, min_count>max_count/음수/float 거부, lot_count 초과 거부, shuffle, lot_id 유일성, 회사 데이터 추정 위반 없음. 미검증: 대규모 성능, T018 이후 단계와의 상호작용.


## 비고

검증 사례: '드문 설비 수 유형' (8절 표).
