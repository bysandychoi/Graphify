# T013 eqp_step 가능 쌍 생성 로직

- 단계: P2 (가상 데이터 생성기)
- 상태: in_progress (작업중)
- 예상 소요: 30분
- 선행 작업: T012
- 참고 problem.md: 3절, 8절

## 설명

공정·step별로 resource가 가능한 설비 집합을 생성한다 (동일 step 내 resource별로 다른 설비 집합 허용).

## 완료 기준

- 같은 step에서 resource별 가능 설비 집합이 다르게 생성될 수 있음

## 쉬운 설명

공정과 step의 각 조합별로, 그 단계에서 필요한 자원들과 각 자원별 가능한 설비 집합을 정의하는 eqp_step 데이터를 생성한다. T012가 생성한 설비 마스터(eqp_floor, resources)를 받아, config의 process_count/step_count로 모든 공정-step 조합을 만든다. 각 조합마다 여러 자원 행을 생성하되, 같은 step 내에서도 자원별로 다른 설비 집합이 가능해야 한다(예: S01에서 R01은 M01/M02, R02는 M03만 가능). 생성된 eqp_step은 이후 단계에서 lot의 가능 쌍 검증과 불일치 감지의 기준 사양으로 사용된다.

## 관련 파일

- `generator/master.py` — T012 산출물(eqp_floor, resources)을 입력받는 기반
- `docs/generator/config.md` — scale.process_count/step_count 정의 및 유효 lot 수 개념
- `docs/virtual_schema/eqp_step_run_spec.md` — 생성할 eqp_step 스키마(process/step/resource/eligible_eqp_ids 필드)
- `docs/virtual_schema/lot_data.md` — candidate_pairs 스키마로 생성 데이터의 검증 기준

## 피드백 로그

### 2026-09-17

테스트 0건: 유일한 완료 기준(같은 step 내 resource별 설비 집합 상이)을 검증하는 코드가 없고, resource_count/eqp_count가 1이면 기준이 조용히 불성립. / generate_eqp_step(...,resource_ids,eqp_ids,...)과 generate_eqp_step_data(config,eqp_ids,resource_ids)의 인자 순서가 서로 뒤바뀜 - 둘 다 list[str]이라 뒤섞어 넘겨도 검증 없이 통과. 마스터 출력을 넘겨주는 배선/호출자가 없어 참조 무결성은 전적으로 호출자 책임. / eqp_step.py:47-54 범위 무음 보정: resources_per_step=(5,2)면 에러 없이 randint(2,2), lo=0이면 행 자체가 사라지거나 eligible_eqp_ids=[] 생성 - 후자는 eqp_step_run_spec.md가 '미결'로 못박은 '빈 배열 vs 행 부재' 상태를 코드가 만들어냄(검증으로 확인). / docstring:8-10의 유일성 보장 주장은 과장 - process_ids에 중복이 있으면 중복 키 생성됨(재현 확인). / resources_per_step/eqp_per_resource=(1,3)은 config.md에 없는 미문서화 매직값이고 config 경로로 조절 불가. 기본 config에서 (process,step) 120개 중 14개는 설비 1대만 제공, 48개만 eligible_eqp_count>=5 지원 - T015/T017/T018 실현 가능성을 말없이 깎음. / rng=None 기본 경로가 Random(config['seed'])를 다시 만들어 master.py가 문서화한 '단일 인스턴스 공유' 계약 위반(난수열 재사용). / P/S id 폭 2 vs master의 4, WHY 주석 없음, 100 넘으면 사전식 정렬 깨짐.

