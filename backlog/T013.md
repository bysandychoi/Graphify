# T013 eqp_step 가능 쌍 생성 로직

- 단계: P2 (가상 데이터 생성기)
- 상태: in_progress (작업중)
- 예상 소요: 30분
- 선행 작업: T012
- 참고 problem.md: 3절, 8절

## 설명

공정·step별로 resource가 가능한 설비 집합을 생성한다 (동일 step 내 resource별로 다른 설비 집합 허용).

## 완료 기준

- 같은 step에서 resource별 가능 설비 집합이 다르게 생성될 수 있음

## 쉬운 설명

공정과 step의 각 조합별로, 그 단계에서 필요한 자원들과 각 자원별 가능한 설비 집합을 정의하는 eqp_step 데이터를 생성한다. T012가 생성한 설비 마스터(eqp_floor, resources)를 받아, config의 process_count/step_count로 모든 공정-step 조합을 만든다. 각 조합마다 여러 자원 행을 생성하되, 같은 step 내에서도 자원별로 다른 설비 집합이 가능해야 한다(예: S01에서 R01은 M01/M02, R02는 M03만 가능). 생성된 eqp_step은 이후 단계에서 lot의 가능 쌍 검증과 불일치 감지의 기준 사양으로 사용된다.

## 관련 파일

- `generator/master.py` — T012 산출물(eqp_floor, resources)을 입력받는 기반
- `docs/generator/config.md` — scale.process_count/step_count 정의 및 유효 lot 수 개념
- `docs/virtual_schema/eqp_step_run_spec.md` — 생성할 eqp_step 스키마(process/step/resource/eligible_eqp_ids 필드)
- `docs/virtual_schema/lot_data.md` — candidate_pairs 스키마로 생성 데이터의 검증 기준

## 피드백 로그

### 2026-09-17

테스트 0건: 유일한 완료 기준(같은 step 내 resource별 설비 집합 상이)을 검증하는 코드가 없고, resource_count/eqp_count가 1이면 기준이 조용히 불성립. / generate_eqp_step(...,resource_ids,eqp_ids,...)과 generate_eqp_step_data(config,eqp_ids,resource_ids)의 인자 순서가 서로 뒤바뀜 - 둘 다 list[str]이라 뒤섞어 넘겨도 검증 없이 통과. 마스터 출력을 넘겨주는 배선/호출자가 없어 참조 무결성은 전적으로 호출자 책임. / eqp_step.py:47-54 범위 무음 보정: resources_per_step=(5,2)면 에러 없이 randint(2,2), lo=0이면 행 자체가 사라지거나 eligible_eqp_ids=[] 생성 - 후자는 eqp_step_run_spec.md가 '미결'로 못박은 '빈 배열 vs 행 부재' 상태를 코드가 만들어냄(검증으로 확인). / docstring:8-10의 유일성 보장 주장은 과장 - process_ids에 중복이 있으면 중복 키 생성됨(재현 확인). / resources_per_step/eqp_per_resource=(1,3)은 config.md에 없는 미문서화 매직값이고 config 경로로 조절 불가. 기본 config에서 (process,step) 120개 중 14개는 설비 1대만 제공, 48개만 eligible_eqp_count>=5 지원 - T015/T017/T018 실현 가능성을 말없이 깎음. / rng=None 기본 경로가 Random(config['seed'])를 다시 만들어 master.py가 문서화한 '단일 인스턴스 공유' 계약 위반(난수열 재사용). / P/S id 폭 2 vs master의 4, WHY 주석 없음, 100 넘으면 사전식 정렬 깨짐.

### 2026-09-17

이전 7개 중 4개만 실질 해소(테스트 21건 추가, 빈 배열 방지, bounds 에러, kwonly). 남은/새 결함: / eqp_step.py:17-21 WHY 주석이 사실과 다름 - '(process,step) eligible_eqp_count 상한 3, 5는 단일 step에서 불가능'은 거짓(기본 config 실측 상한 8, 120그룹 중 52개가 >=5 지원). 게다가 제시한 대안 '여러 step에 걸친 candidate_pairs 합산'은 lot_data.md:13-14,22,75-78(lot은 current_step 하나, 다음 step 추론 금지)을 정면 위반 - 지금 active인 T014를 잘못된 방향으로 유도한다. / 유일성 검증이 반쪽: resource_ids 중복 미검증 -> (P,S,R) 중복 키 생성(resource_ids=['R1']*3, seed 0 재현). eqp_ids 중복 미검증 -> eligible_eqp_ids에 같은 id 2회(seed 1 재현), spec:41 '집합으로 취급' 위반. docstring:8-11의 '아래 함수들이 검증한다'는 여전히 과장. / 검증 비대칭: resource_ids/eqp_ids 빈 리스트는 에러인데 process_ids/step_ids 빈 리스트는 조용히 [] 반환. bounds 검증이 루프 안(_sample_count)이라 빈 입력이면 resources_per_step=(9,2), eqp_per_resource=(0,0)도 통과 - test_rejects_invalid_bounds가 못 잡는 구멍. process/step 위치 인자 맞바꿈도 여전히 무음 통과(실측). / pipeline.py 테스트 0건: 이전 리뷰의 '배선 없음/참조 무결성 호출자 책임'을 고치려고 만든 모듈인데 참조 무결성/rng 공유/재현성 어느 것도 회귀 테스트 없음(수동 확인은 셋 다 참). {**master,**eqp_step} 키 충돌 무음 덮어쓰기. config.md:86-95는 seed 단일 인스턴스 계약 소유자를 master.py로 못박았는데 rng 생성이 pipeline.py로 옮겨갔음에도 문서 미갱신, README '실행 방법 미정'도 그대로. eqp_step.py:93의 'generate_master_data가 만든 rng'는 사실과 다름(pipeline:15가 만든다). / 완료 기준 테스트(38-53)는 '가능'만 확인 - 이전 피드백의 조용한 불성립(eqp_ids 1개면 전 resource 동일 집합, 에러/경고 없음) 은 코드도 테스트도 없음. generate_process_ids/step_ids 직접 테스트 0건이라 id 폭 2(master는 4)가 아무데도 고정 안 됨. master.py:9-12가 못박은 고정폭 불변조건(8대와 80대가 같은 M0001)도 테스트 없음. generate_master_data 자체 테스트 0건. test_keyword_only_ids_prevent_positional_mixups는 파이썬 문법 동어반복. / 이전 지적 7번(P/S 폭 2, WHY 주석, 101개 이상 사전식 정렬 P100<P11) 전혀 미반영. eqp_per_resource는 generate_eqp_step에만 kwarg로 뚫려 있고 data/pipeline 경로엔 통로 없어 config 조절 불가(5번 절반만 해결). pylint R0913(eqp_step.py:57, 7/5).

