# T014 lot_data 가능 쌍 생성 로직

- 단계: P2 (가상 데이터 생성기)
- 상태: done (완료)
- 예상 소요: 30분
- 선행 작업: T013
- 참고 problem.md: 3절

## 설명

eqp_step을 참고해 lot별 (eqp, resource) 가능 쌍을 생성한다. 설비 수는 쌍의 중복 없는 eqp 집합 크기로 계산한다.

## 완료 기준

- 설비 수가 eqp 집합 크기와 항상 일치
- (M01,R01),(M02,R02) 존재 시 (M01,R02)가 암시되지 않음

## 쉬운 설명

각 lot마다 현재 처리할 step에서 사용 가능한 (설비, resource) 쌍들을 만드는 작업입니다.

**구체적으로 뭘 하나요?**
1. config에서 생성할 lot 수를 받습니다
2. 각 lot마다:
   - 처리할 step을 정합니다 (S01, S02 등)
   - T013이 생성한 eqp_step에서 해당 (공정, step, resource) 행들을 찾습니다
   - 각 행의 eligible_eqp_ids(가능 설비 목록)와 resource를 조합해 (설비, resource) 쌍을 만듭니다
   - 이 쌍들을 lot의 candidate_pairs에 넣습니다
3. 각 lot마다 candidate_pairs에 나타나는 중복 없는 설비의 개수를 eligible_eqp_count로 계산합니다
   - (M01,R01), (M01,R02) → eligible_eqp_count = 1 (M01만)
   - (M01,R01), (M02,R02) → eligible_eqp_count = 2 (M01, M02)

**주의할 점:**
- 명시된 쌍만 만듭니다. (M01,R01), (M02,R02)가 있어도 (M01,R02)를 자동으로 만들면 안 됩니다
- candidate_pairs의 중복 여부와 정렬은 구현 시 정의합니다

**완료 기준:**
- eligible_eqp_count가 candidate_pairs의 중복 없는 eqp 개수와 항상 일치
- 암시된 쌍이 없음

## 관련 파일

- `generator/master.py` — T012 산출물(eqp_floor/resources) 구조 참고
- `generator/eqp_step.py` — T013 산출물(eligible_eqp_ids) 구조와 생성 방식 참고
- `docs/virtual_schema/lot_data.md` — candidate_pairs와 eligible_eqp_count 정의 확인
- `docs/virtual_schema/eqp_step_run_spec.md` — eqp_step 스키마와 (process, step, resource) 조회 방식
- `docs/generator/config.md` — scale.lot_count, step_count 등 config 구조
- `problem.md` — 3절에서 lot과 가능 쌍의 의미 정의

## 피드백 로그

### 2026-09-17

adversarial 리뷰(2026-09-17): (1) [최중대] lot_data.py의 step_id 단독 합집합 규칙이 eqp_step_run_spec.md가 '임의로 합집합/우선순위를 정해 넘기지 않는다'고 못박은 미결 조인 키를 사실상 확정한다. 실측: 기본 규모 10,000 lot 전부가 어떤 단일 (process,step) 행 묶음으로도 설명 불가(10000/10000) -> (process,step) 조인 해석에서는 100% 사양 불일치 데이터. lot별로 process 하나를 골라 쓰면 두 해석 모두와 모순되지 않는다. (2) 테스트가 결함을 못 잡는다 — 변이 3종(lot_data가 공유 rng 무시 / candidate_pairs 항상 빈 배열 / round_half_up을 표준 round·절삭으로 교체) 모두 48개 테스트 전원 통과. 즉 seed 공유 계약, 파이프라인 배선, 사사오입 규칙 중 어느 것도 테스트로 고정돼 있지 않다. test_pipeline의 참조 무결성 테스트는 빈 배열이면 공허하게 통과하고, test_lot_data의 eligible_eqp_count 테스트 2개는 헬퍼 구현식을 그대로 재작성한 항진명제다(lot_data.md의 M03·2쌍->1 예시 미검증). (3) _util.round_half_up: math.floor(v+0.5)는 0.49999999999999994를 1로 올려 '0.5 이상만 올림' 규칙을 어기고, effective_lot_count는 부동소수 곱 후 반올림이라 lot_count=1500, factor=0.009(정확히 13.5)에서 14가 아닌 13을 낸다 — config.md 사사오입 계약 위반. test_util.py 자체가 없다. (4) 모든 lot이 step별로 완전히 동일한 candidate_pairs를 갖는다(10,000 lot에 서로 다른 집합 20개=step_count, eligible_eqp_count 16~29). eqp_step.py가 명시한 상한 9를 process 합집합으로 우회한 셈이고, config.md 예시 분포(1/2/3)와 문서가 유효하다고 한 빈 candidate_pairs(=0) 케이스를 이 설계로는 만들 수 없다 — T015/T017 사전 차단. (5) _LOT_ID_WIDTH=6은 근거 주석 없고 master/eqp_step의 4자리, lot_data.md 예시(LOT_V0001)와 모두 어긋나며 폭을 고정하는 테스트도 없다. (6) step_ids 중복 미검증(T013에서 4개 목록 전부 검증한 것과 비대칭), eqp_step_rows 스키마 미검증(KeyError), pipeline이 generate_step_ids를 eqp_step 내부와 따로 한 번 더 호출해 조용히 어긋날 수 있음, _merge_disjoint 충돌 분기 미테스트, T014가 정한 생성 규칙이 docs/에 전혀 기록되지 않음(config.md 456행이 T014에 위임한 정책).

