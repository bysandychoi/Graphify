# CLAUDE.md

이 저장소에서 작업하기 전에 **AGENTS.md**를 먼저 읽어라. 공통 작업 규칙
(코드 작성 규칙, backlog.json 다루는 법, 서브에이전트 사용법, 훅 동작)이
모두 거기 정리돼 있다. 이 문서는 규칙을 옮겨 적지 않는다 — 내용이 바뀌면
AGENTS.md만 고치면 되도록, 여기서는 어디를 보라고 가리키기만 한다.

문제 정의는 `problem.md`, 작업 목록은 `backlog.json`(AGENTS.md "backlog.json은
CLI로만 다룬다" 절 참고, 직접 열지 않음).
